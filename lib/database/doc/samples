# Default TIMEZONE:

query("SET TIMEZONE TO 'Asia/Tehran';");
==================================================================================================================
# get type of data:

SELECT pg_typeof(fitnessStatusList->'front') from "UserData";
===================================================================================================================