===================================================================================
UPDATE "CustomerModels" SET IsAnswered = NULL WHERE 1=1;
===================================================================================
WITH t AS (
    UPDATE products SET price = price * 1.05
    RETURNING *
)
SELECT * FROM t;
===================================================================================
